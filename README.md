# ZMZ Nest Template

这是一个功能完整的 NestJS 后端项目模板,包含开箱即用的最佳实践配置。

## ✨ 特性

- 📖 **Swagger 文档** - 自动生成 API 文档
- 🗄️ **TypeORM + PostgreSQL** - Migration 管理数据库变更
- ⚡ **Redis 缓存** - 封装常用 Redis 操作
- 🔒 **环境变量验证** - class-validator 确保配置安全
- 🛡️ **全局异常处理** - 统一错误返回格式
- 📦 **统一响应结构** - 自动包装 API 响应
- ✅ **请求验证** - 全局 ValidationPipe
- 👥 **用户模块示例** - 完整的 CRUD 演示

## 🚀 快速开始

### 1. 使用模板创建新项目

点击页面右上角的 "Use this template" → "Create a new repository"

### 2. 克隆到本地

```bash
git clone https://github.com/YOUR_USERNAME/YOUR_REPO_NAME.git
cd YOUR_REPO_NAME
```

### 3. 运行初始化脚本

```bash
# 安装依赖
pnpm install

# 运行初始化脚本(会自动替换项目名称、描述等)
pnpm run template:init
```

### 4. 配置环境变量

```bash
# 复制环境变量配置文件
cp .env.development .env

# 编辑 .env 文件,填写数据库和 Redis 配置
```

### 5. 启动项目

```bash
# 运行数据库迁移
pnpm run migration:run

# 启动开发服务器
pnpm run start:dev
```

访问:
- API: http://localhost:3000/api
- Swagger 文档: http://localhost:3000/api/docs

## 📁 项目结构

```
src/
├── common/                   # 公共模块
│   ├── decorators/          # 装饰器(@ResponseMessage)
│   ├── enums/               # 枚举(ErrorCode)
│   ├── exceptions/          # 自定义异常(BusinessException)
│   ├── filters/             # 全局异常过滤器
│   └── interceptors/        # 响应拦截器
├── config/                  # 配置模块
├── database/                # 数据库模块
├── migrations/              # 数据库迁移文件
├── redis/                   # Redis 模块
├── users/                   # 用户模块(示例)
└── main.ts                  # 应用入口
```

## 🔧 初始化脚本说明

`pnpm run template:init` 会执行以下操作:

1. 提示你输入新的项目名称
2. 自动替换 `package.json`、`README.md`、`.env.*` 等文件中的项目名称
3. 删除模板相关文件
4. 清理 git 历史(可选)

## 📝 开发指南

详见项目根目录的 [README.md](README.md) 和 [CLAUDE.md](CLAUDE.md)

## 🤝 贡献

欢迎提交 Issue 和 Pull Request!

## 📄 许可

MIT
